what is docker?
	docker is devops tool used to run ,build ,manage etcc
	application containers.by this docker tool we can do 
	the workflow is same on a personal computer,server,
	production server.
Docker image
	docker image is an BLUEPRINT or TEMPLATE fot creating
	a container.
Docker containr
	docker container is running istance of image.
Docker file
	docker file is text file containsinstruction to 
	 create a Docker image.
Dokcer volume 
	a docker volume can store persisting data even if the
	 containers are have been removed the data remains.
Docker network 
	a network  is used to communicate with same container
	 names by user-defined network with a automated dns 
	by converting name into ip address network 
	communication will be done.
Docker compose
	docker compose is used to run the all cmd's at one 
	time a .yml file.docker compose is used manage 
	multiple containers work together. 


## Commands I Practiced

- docker build
- docker run
- docker ps
- docker ps -a
- docker images
- docker stop
- docker start
- docker exec
- docker volume create
- docker volume ls
- docker network create
- docker network connect
- docker compose up -d
- docker compose ps
- docker compose logs
- docker compose down
